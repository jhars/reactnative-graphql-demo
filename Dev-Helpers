$ cd ios
$ bundle install && bundle exec pod install

(remove node_modules)

$ yarn install

$ amplify pull d362ue9l9i9kh7

$ yarn ios

$ yarn add @aws-amplify/ui-react-native aws-amplify @aws-amplify/react-native react-native-safe-area-context @react-native-community/netinfo @react-native-async-storage/async-storage react-native-get-random-values

$ npx pod-install

$ yarn add @react-navigation/native
$ yarn add @react-navigation/stack

yarn add @react-navigation/native @react-navigation/stack

$ yarn ios


$ yarn add @react-navigation/drawer 
$ yarn add react-native-gesture-handler react-native-reanimated

Followed tutorial to add drwaer nav: https://reactnavigation.org/docs/drawer-navigator/?config=static#drawertype

(again)
$ npx pod-install ios

ANDRORID config for Navigation packages:
Main Activity
@Override
protected void onCreate(Bundle savedInstanceState) {
  super.onCreate(null);
}
++++++++++++++++++++++
yarn add react-redux axios @reduxjs/toolkit redux-logger

redux-thunk 
